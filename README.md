# whip-for-claude
A native macOS menu bar app. When Claude Code (or any CLI) is going too slow, whip it into shape. Click the tray icon, crack the whip — it sends Ctrl+C and types a random “go faster” line into the focused app.
